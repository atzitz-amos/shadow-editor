/**
 *
 * @author Atzitz Amos
 * @date 11/1/2025
 * @since 1.0.0
 */
export class PopupFactory {
    public static createTooltipPopup(editor: any, message: string, xy: { x: number, y: number }, bounds: {
        width: number,
        height: number
    }): void {

    }
}
