/**
 * Represents a block widget. Block widgets are displayed as one div instead of multiple spans, which can
 * lead to weird spacing if the range does not align with block boundaries.
 * They take up space in the document flow and are used to show larger components
 * that span more lines than what {@link InlayWidget} are allowed to.
 *
 * @author Atzitz Amos
 * @date 10/24/2025
 * @since 1.0.0
 */
export abstract class BlockWidget {
    // TODO

}
