/**
 * Holds the ASTTree, SyntaxTree, Lexical Tokens, and ExportedModel of a file.
 * All fields except ExportedModel are computed on-demand and cached for efficiency.
 * ExportedModel is continually updated as the user edits the file and is saved for persistence, so that there
 * is no need to recompute the whole project when reopening it.
 *
 * @author Atzitz Amos
 * @date 11/12/2025
 * @since 1.0.0
 */
export class FileIndex {

}
